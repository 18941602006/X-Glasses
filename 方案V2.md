# 导盲眼镜方案 V2：开源改造、无 SLAM、LocateAnything 找物

状态：供用户确认的设计方案，不是开发授权。未安装依赖、下载模型、编写固件或应用，也未做实机测试。

## 1. 决策摘要

- 电脑原型以 AI-FanGe/OpenAIglasses_for_Navigation 为优先改造底座，先审核源码和依赖、锁定提交，再决定实际保留的模块。不承诺整仓可直接运行。
- 眼镜首选 XIAO ESP32-S3 Sense，配前向多区 ToF 和六轴 IMU；手机目标仍为骁龙 8 系列 Android。
- 不做 SLAM、三维建图、回环或室内自主定位。导航由地图步行路线与短时局部感知组成。
- 找物识别按用户要求使用 LocateAnything；首次定位和失跟后的重新定位由它负责，连续帧使用轻量跟踪，手部使用 MediaPipe。
- 避障、交通灯与 OCR 各用适配的专用模型，不将所有任务交给 LocateAnything。
- 定位识别、连续跟踪、拿取确认是三个独立环节；跟踪器不能代替用户指定的识别模型。

## 2. LocateAnything 核实结果及约束

本轮按 NVIDIA 官方 LocateAnything 项目制定方案：

- 源码：https://github.com/NVlabs/Eagle/tree/main/Embodied
- 模型：https://huggingface.co/nvidia/LocateAnything-3B
- 官方介绍：https://research.nvidia.com/labs/lpr/locate-anything/
- 权重许可：https://github.com/NVlabs/Eagle/blob/main/LICENSE_MODEL

若用户指的是其他同名项目，应以用户提供的仓库链接为准。

官方发布为 3B 视觉语言定位模型，输入图像与文字，输出标签及二维框/点；不是距离传感器，也不是直接输出抓取执行动作的系统。公开权重目前不原生支持 visual prompt inference，因此首版使用文字描述找物，不承诺拿参考照片找同款。中文描述效果需要实测，可评估保留颜色、关系和数量信息的提示词转换。

源码 README 标示 Apache-2.0，模型权重另受 NVIDIA 非商业许可约束，限学术或非营利研究；商业原型也不能仅因尚未销售就假定符合许可。项目用途必须在使用权重前确认，若不适用则先获取授权，不擅自替换模型。

官方推理以 Linux、Transformers、BF16 和 NVIDIA GPU 为主。本次资料未提供可直接部署至骁龙 Android 的成熟路径。官方 BPS 是每秒输出框数，不是视频 FPS；不能用它承诺眼镜实时帧率。

手机迁移设置硬门槛：验证视觉编码器、动态图像处理、结构化解码、量化精度、算子支持、峰值内存、首次定位延迟和持续温升。仅把模型转为 ONNX 不等于完成迁移。

保持两条明确边界：

1. 最终纯手机本地运行仍是目标，LocateAnything 本地适配需要专项验证。
2. 若验证失败，电脑端原型不能视为最终完成；是否允许远端按需识别，必须重新取得用户同意，不能默认采用云端或另一模型。

## 3. 开源复用与改造范围

| 功能 | 优先来源 | 保留/利用 | 必须改造或补充 |
| --- | --- | --- | --- |
| 电脑服务与调试页面 | OpenAIglasses_for_Navigation | FastAPI、WebSocket、模式组织、监控与录制思路 | 数据时间戳、依赖隔离、非阻塞调度、告警优先级、断连状态 |
| 盲道与斑马线 | 同上 | 原模型作为对照基线 | 权重及标签核验、眼镜视角测试、通用步行区域分割 |
| 找物识别 | NVlabs/Eagle/Embodied | 官方 LocateAnything worker 与模型 | 统一目标结果格式、异常/无目标处理、语义查询、请求过期处理 |
| 手部引导 | 原项目和 MediaPipe 官方示例 | 手部关键点与交互思路 | 轻量跟踪、同一目标保持、遮挡后重定位、多证据拿取确认 |
| 路面分割 | PaddleSeg PP-LiteSeg | 模型、训练及推理流程 | 本地眼镜视角标注、类别映射、时序稳定和 ToF 融合 |
| 文字阅读 | PaddleOCR | 检测、识别、Android 部署参考 | 清晰度检查、阅读顺序、重复抑制和朗读控制 |
| 手机链路 | MyEyes 为架构参考 | JPEG 接收、Android 推理、音频链路设计 | 在权限明确前不直接复制其代码；手机适配不承诺复用 Python UI |
| 地图导航 | 所在地区的地图步行 SDK | 路线规划和转向事件 | 与局部风险协调；中国大陆优先评估高德 |

原项目地址：https://github.com/AI-FanGe/OpenAIglasses_for_Navigation
PaddleSeg：https://github.com/PaddlePaddle/PaddleSeg
PaddleOCR Android：https://github.com/PaddlePaddle/PaddleOCR/blob/main/docs/version3.x/inference_deployment/cross_platform/android_deployment.md
MediaPipe：https://developers.google.com/edge/mediapipe/solutions/vision/hand_landmarker/android
MyEyes：https://github.com/erjon-musa/myeyes

项目原有“绿灯可以通行”话术不沿用；不能照搬聊天模式暂停全部感知的行为。核心近距感知常驻，聊天、OCR、找物按需调度。

## 4. 硬件清单与连接原则

以下是设计级清单，不是已审核可下单的具体商家 BOM，不提供未核实报价。

| 部件 | 数量 | 选择与要求 |
| --- | --- | --- |
| XIAO ESP32-S3 Sense | 1 | 带相机扩展板、天线、散热件；核对实际相机批次 |
| 摄像头 | 随板 1 | 当前官方列 OV3660；先不额外购买，确认近距文字与远处信号灯效果后再升级 |
| VL53L5CX 多区 ToF 转接板 | 1 | 确认供电及 I2C 电平与 3.3V 主控兼容，优先有原理图及驱动的模块，不买裸芯片作首版 |
| LSM6DSOX 六轴 IMU 转接板 | 1 | 3.3V 兼容 I2C，测量抬头低头与短时转动；不作为绝对航向传感器 |
| 震动马达及匹配驱动电路 | 1 套 | 电压电流与电机类型匹配，不用 GPIO 直接驱动；若选 ERM，包含合适开关及续流保护 |
| 实体功能按键 | 1–2 | 模式/开始、取消/重复播报，最终根据可用 GPIO 确认 |
| 开放式耳机 | 1 | 连接电脑/手机播放语音；不要求 S3 作为普通蓝牙音频发射器 |
| USB 电源、数据线 | 1 套 | 台架阶段使用；佩戴电池方案等功耗实测后定 |
| 可调安装支架、线材、连接件 | 1 套 | 相机与 ToF 固定相对姿态，热源不接触皮肤，线束需应力释放 |

Sense 自带数字麦克风，首版不再买独立麦克风。为先验证视觉，可暂用电脑麦克风；后续接入眼镜麦克风上行。完整音频回传眼镜作为后续扩展，不增加首版硬件负担。

ToF 与 IMU 可规划共享 I2C，总线地址、电平、上拉和带宽须核验；ToF 初始化和上传任务不得阻塞相机。GPIO 分配要检查相机、麦克风、SD 和启动引脚占用；首版不依赖 SD 卡存储。协议同时携带采样时间、序号、测量有效性、设备状态。

单个前向 ToF 仅提供覆盖区域内的近距信息。不把无回波当空旷；不宣称能覆盖所有脚下落差。后续若增加向下 ToF，需要单独评估头动、视场、地面几何和互扰，不能仅因加了传感器就承诺防跌落。

官方资料：

- https://wiki.seeedstudio.com/xiao_esp32s3_getting_started/
- https://www.st.com/en/imaging-and-photonics-solutions/vl53l5cx.html
- https://www.st.com/resource/en/datasheet/vl53l5cx.pdf
- https://www.st.com/en/mems-and-sensors/lsm6dsox.html

## 5. 运行架构及六项功能

眼镜上传图像/ToF/IMU → 电脑（后续手机）统一输入层 → 常驻近距风险链路及按需任务 → 提示仲裁 → 耳机/震动。

电脑端将 LocateAnything 放入独立进程/环境，防止与原项目的 Python、Torch、Transformers 等依赖冲突，保留清晰服务接口。它不占据实时处理线程，重定位期间播报“正在查找”，不继续使用过期框引导伸手。

### 行进避障与道路分割

PP-LiteSeg 提取候选步行区域；保留原盲道/斑马线模型作为专项模块和对照。ToF 用标定及采样时间对齐后形成局部风险，结合视觉障碍排除候选通道，输出左/中/右方向提示或停止/未知。

不做全局三维地图；仅保留短时状态用于抖动抑制和方向连续性。道路像素不等于行人可走区域；默认不将车行道标成可走。没有可信距离/尺度时，不断言通道足够宽。IMU 不能独自给出身体行进方向，明显转头/不确定时降级。

### LocateAnything 找物与拿取

用户描述 → LocateAnything 首次定位 → 必要时多目标澄清 → 初始化轻量单目标跟踪 → MediaPipe 手部引导 → 目标丢失/目标变化时暂停并重新定位 → 用户确认拿取。

建议初期使用 OpenCV 光流或 CSRT 类跟踪方案做验证，移动端再据耗时选择对应实现；不默认 ByteTrack 能在没有持续检测输入时完成任意对象跟踪。

接近物体后使用短句和节流提示。二维框/手部关键点不能提供可靠手物三维间距；ToF 粗分区只在空间对应可信时辅助估距。拿取成功需物体随手移动、连续证据与用户确认，不凭握拳独断。首版限定静止桌面物品与停步操作。

### 红绿灯

以已有交通灯模型为起点，核验行人灯类别、状态及对应斑马线关系，加入时序确认和未知状态。只报告检测到的信号，不宣布安全过街。看不清或无法关联时明确拒绝判断。

### 导航

手机地图定位/步行路线提供全局转向，眼镜负责局部风险。地图指令不能覆盖停止告警；GPS 不能判断是否在盲道中央。电脑阶段回放路线或模拟转向事件，不为此提前开发手机端。不做室内自主导航。

### AI 对话

复用原项目的语音/对话接口组织，具体服务保持可替换。按键触发，用户请求后才上传必要图像。安全链路不依赖云端，AI 不直接执行行进决策。API 密钥不写死在公开源码/固件。

### OCR

清晰度检查 → 高分辨率照片 → PaddleOCR → 阅读顺序整理 → 原文朗读。不要用 LocateAnything 的文字位置检测代替全文 OCR。聊天式解释与原文朗读分开，避免补造识别不出的内容。

提示优先级：紧急近距/感知失效 > 局部方向 > 地图转向 > 找物及阅读 > 聊天。所有提示带有效期，实时任务仅保留最新帧。

## 6. 实施阶段与验收门槛

| 阶段 | 交付物 | 核心验收 |
| --- | --- | --- |
| P0 依赖和许可核查 | 底座版本、权重来源、LocateAnything 单图验证方案 | 许可用途适用；确认可用推理路径，不凭“游戏本”保证显存够用 |
| P1 眼镜链路 | JPEG、ToF、IMU、按键、基础告警和录制回放 | 连续运行、时间对齐、旧帧丢弃、断连告警 |
| P2 核心行进 | 路面分割、ToF 融合、受控场景风险与方向提示 | 实测漏报、误报、延迟，阳光失效可降级 |
| P3 找物拿取 | LocateAnything、跟踪、手部引导与确认闭环 | 无物/多物/遮挡/失跟/错误目标均有处理；记录定位等待与拿取成功率 |
| P4 辅助功能 | OCR、对话、行人灯、地图事件协调 | 语音可抢占；不输出安全过街承诺；断网降级 |
| P5 Android 迁移 | 本地轻量链路及 LocateAnything 专项可行性报告 | 同一回放集一致性、后台运行、内存和温升；本地 LocateAnything 不通过则不能宣称最终目标完成 |

应在电脑功能扩展前就完成 LocateAnything 手机迁移路线的文档/算子风险预审，避免最后才暴露架构冲突；手机应用开发仍按用户前期不需要手机的要求后置。

建议首版测试目标：640×480 图像约 10fps，ToF 8×8 约 15Hz；它们是预算配置，不是承诺性能。以端到端 P95 延迟、过期提示、传感器有效率与风险漏报为重点。300ms 可作为视觉告警优化目标，但不等于整体系统已安全。LocateAnything 首次定位独立计时，不算作实时跟踪帧率。

数据按实际眼镜视角采集，训练测试按路线/时段分隔；覆盖逆光、暗光、细杆、深色物体、玻璃、悬空障碍、抬头转头与路沿。首期受控、有陪同且保留盲杖，不安排独立道路或过街试用。

## 7. 本轮待确认

- 是否认可以上开源主底座、硬件方向和功能拆分。
- LocateAnything 是否确指 NVIDIA 的 NVlabs/Eagle/Embodied。
- 模型使用是否属于其许可允许的学术/非营利研究；商业用途先解决授权。
- 不将远端 LocateAnything 作为已批准方案；如手机本地适配不成立，另行请求用户决策。
- 本轮仅设计和记忆文档落盘，等待用户明确开发指令。
