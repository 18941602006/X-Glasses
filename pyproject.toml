[tool.ruff]
target-version = "py311"
line-length = 100
include = ["tools/**/*.py", "tests/**/*.py", "server/**/*.py"]
extend-exclude = ["vendor-cache", "models", "weights", "recordings"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.format]
quote-style = "double"
