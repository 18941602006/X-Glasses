# 导盲眼镜技术方案 V3

状态：现行技术设计基线，已获用户开始施工指令；当前先按用户最新要求完成施工规范 V3 对齐，尚未实现业务功能。V3 覆盖 V2 中关于 Wi-Fi 主链路、盲道/斑马线专项识别和旧前端的设计；V2 保留作历史。工程执行规则以《X-Glasses施工规范V3.md》为准，性能及许可关卡仍须实测/核实。

## 1. 本轮确定的变化

- 删除盲道专项识别、沿盲道导航、斑马线识别/对齐、自动过街流程及对应模型加载、状态、接口和页面入口，不只是隐藏按钮。
- 保留道路/步行区域语义分割、近距避障、红绿灯状态识别、地图步行导航、LocateAnything 找物与拿取、OCR、AI 对话。
- 眼镜与电脑/手机的业务数据采用 USB 数据线，不以 Wi-Fi/BLE 为运行前提。
- 手部辅助升级为视觉目标、手部与 ToF 的时序融合流程，不照搬旧的二维重叠/握拳判断。
- 电脑调试前端重新设计；最终手机端采用无障碍优先的独立交互，不缩小照搬电脑面板。
- 不做 SLAM，不提前开发手机 App，不自行更换 LocateAnything；按现行施工规范分 Phase 实施。

## 2. 架构及开源改造策略

XIAO S3 Sense 的摄像头、ToF、IMU、按键 → 原生 USB 设备 → 电脑/手机 USB 接收层 → 标准化帧与事件总线 → 实时感知及按需辅助 → 提示仲裁 → 计算端耳机/眼镜震动。

电脑通过 USB 接收的 JPEG 在 Python 后端解码。新的浏览器界面通过 localhost HTTP/WebSocket 连接 Python 服务；这里的 WebSocket 是计算机内部前后端通信，不是眼镜与电脑的无线传输。Android 使用 USB Host 接收数据并直接交给本地模型/业务层，不要求持续运行电脑服务。

OpenAIglasses_for_Navigation 降为“后端可复用模块来源”，不再整套沿用其主流程：

| 子系统 | 处理方式 |
| --- | --- |
| FastAPI、录制、通用状态组织、语音接口 | 审核后复用或抽取；剥离旧盲道/斑马线耦合 |
| 原固件 Wi-Fi 推流 | 替换为 ESP-IDF/esp_tinyusb 原生 USB CDC，保留可复用采集逻辑 |
| 原盲道/斑马线工作流 | 从运行范围移除；不依赖其权重或导航状态 |
| 找物识别 | NVIDIA LocateAnything worker，独立进程/依赖环境 |
| 手部检测 | MediaPipe；上层融合、反馈和拿取状态机改造 |
| 道路分割 | PaddleSeg PP-LiteSeg 候选，以实际眼镜视角验证/微调 |
| 红绿灯 | 可参考旧检测模块，独立核验权重和类别，取消过街决策 |
| OCR | PaddleOCR 及官方移动端示例 |
| 新电脑前端 | 建议 React + TypeScript + Vite，复用成熟组件，重建信息架构和状态契约 |
| 新手机前端 | Kotlin + Compose + Android USB Host；与电脑共享协议/业务语义，不宣称复用同一 UI 代码 |

先核验依赖、许可和可运行性再选具体代码，保留来源与许可证。拆成模块不是从零重写所有功能，也不承诺固定复用比例。

## 3. USB 技术路线

### 3.1 角色和物理连接

- ESP32-S3：USB Device。
- 电脑：USB Host；数据线连开发板的原生 USB 接口。
- Android：USB Host/OTG；USB-C 数据线连接 Sense，或兼容的 OTG 转接方式。
- 同一时刻只连接一个计算主机，不使用分线将电脑和手机同时当主机。
- 手机 USB Host、访问授权、供电上限、休眠行为必须在具体机型验证；“骁龙 8 系列”不能替代这些检查。

S3 原生 USB 为 Full-Speed 12Mbps，不是 480Mbps High-Speed。USB-OTG 与 USB-Serial-JTAG 默认共享内部 PHY，不能假设同一个物理接口能同时用于两套控制器。首版不烧写 eFuse，不加外部 PHY；预留按键进入下载模式的恢复流程，运行日志用协议包或独立 UART 调试，不把裸日志混入图像流。

### 3.2 推荐：原生 USB CDC-ACM + 多路二进制协议

使用 esp_tinyusb/ESP-IDF 现有 USB 设备栈，不自写 USB 驱动。

- 电脑端用 pySerial 类接口接收 CDC 数据。
- Android 复用 mik3y/usb-serial-for-android 的 CDC 驱动，或基于官方 USB Host API，在读写线程解析数据，不占用 UI 线程。
- CDC 看起来像串口，但本方案走原生 USB bulk 数据端点，不经过 CH340/CP2102 等 UART 桥。虚拟串口的 line coding 波特率不等同于原生 USB 吞吐，仍需确认固件处理与主机配置。
- 如基准表明 CDC 实现开销成为瓶颈，再评估 vendor-specific bulk；它不会突破 S3 的 12Mbps 物理上限。

UVC+CDC 是备选：Espressif 有开源 USB 摄像头示例，但 UVC 通道与传感器通道的同步、复合描述符和 Android 兼容会增加验证量。不能认为 CameraX 自动支持所有外部 UVC 设备，也不能认为 UVC 必然比 CDC 快。首版选单一 CDC 协议，减少两端适配变量。

### 3.3 消息和调度

上行：JPEG 分片、ToF 分区距离/目标状态/质量数据、IMU、按键、设备与供电状态；后续可加麦克风音频。
下行：开始/暂停采集、分辨率和 JPEG 质量、拍照、震动模式、模式配置、同步请求。

协议头建议包含 magic、协议版本、session_id、消息类型、序号、设备采样时间、载荷长度与校验。JPEG 分片额外包含 frame_id、总长度与偏移；组帧后才交给解码器。长度设硬上限，异常包可重同步。校验用于应用协议完整性，不声称 USB 本身不具备错误检测。

调度规则：

- 每次仅向 USB 栈提交有限数据，JPEG 分片之间允许插入状态、传感器和控制回应。
- 旧图像只丢弃完整未发送帧，或发送明确取消标记；不能从已经发出的帧中间无声删除字节。
- 短写/短读按实际长度续传；超时或不完整帧丢弃重同步，不无限等待。
- 用 ACK/request_id 确认重要控制命令；图像过期直接丢弃，不靠反复重传旧图像追求零丢帧。
- 主机持续读取 USB，解码与推理使用独立有界队列，仅保留最新可用帧。
- 设备采样时间与主机时间做偏差/往返延迟估计；同一 USB 线不代表传感器同时曝光/采样。
- JPEG 时间优先对应采集/曝光时刻，ToF 对应测量周期，不能用收到 USB 数据的时间替代。超过标定的同步容差拒绝做精细融合。

### 3.4 带宽预算与模式

起步测试目标为 640×480 JPEG、约 10fps，ToF 8×8 约 15Hz，IMU 50–100Hz；均为设计预算而非实测承诺。

示例：40KB/JPEG × 10fps = 400KB/s；16kHz、16bit、单声道 PCM 为 32KB/s；ToF/IMU 和包头再占部分带宽。JPEG 大小随画面细节变化，必须测最坏场景、持续吞吐及 P95 延迟，不能只测平均帧长。只有采集、USB、解码和推理整体留有余量才启用目标配置。

行进用低延迟预览；停步找物与 OCR 触发较清晰单张照片，再恢复预览。照片分片期间传感器消息继续传输，ToF 本地任务不能阻塞。首版不用未压缩 RGB 连续传输，也不承诺高清高帧率。

### 3.5 供电与断连

台架由电脑 USB 供电；手机阶段先测 OTG 给整机供电的可行性，不只检查主板空载功耗。原型先不增加眼镜电池和复杂充电路径。

若全机由 USB 供电，拔线会导致眼镜掉电，眼镜无法继续震动或感知。断连提示应由仍有电的电脑/手机通过耳机/手机振动发出，并立即废弃旧方向提示。仅数据中断但 ESP32 仍供电时，才保留本地 ToF 基础告警。

若后续要求拔线眼镜仍工作，需要带保护、供电隔离与电源路径管理的独立电源方案；不能用 Y 线并联两个电源。边给手机充电边做 OTG 需要实测支持该角色组合的 PD Hub，不能默认普通分线器可用。线束沿镜腿至衣领固定，留应力释放，避免拉扯 USB 座或缠绕颈部。

## 4. 手部融合升级：必须适配、调整和升级

旧方案是目标二维位置加手部关键点/手势。新方案加入 LocateAnything 与 ToF 后，需要重新定义数据契约、标定、融合、状态和提示规则，而不只是增加距离播报。

### 4.1 三种数据各自负责什么

- LocateAnything：依据文字描述给出二维目标框/点。其结果绑定源 frame_id、查询与采样时间；晚到结果不能直接叠加到当前画面。需要通过已缓存帧的跟踪关系传播，无法可靠传播就重新采图定位。
- 连续跟踪：在两次重识别之间更新同一个目标，处理漂移/遮挡；失跟暂停引导，不偷偷换成相邻物体。
- MediaPipe：手部关键点与可见性。其相对深度/手局部坐标不能直接与 ToF 距离相减。
- ToF：眼镜传感器视场内各分区的测距和有效性，不直接给出“手距离杯子多少厘米”。

### 4.2 融合前的标定

相机内参和畸变、ToF 分区朝向/有效视场、相机与 ToF 相对位姿、分辨率/裁剪变换、传感器时间偏差均须标定或测定。镜头/ToF 固定在刚性支架上，近距时视差更明显，不能拿二维框中心简单线性映射一个格子。

将 ToF 分区视为带角度范围和测量不确定性的区域，投影到相机视图；只在多个有效分区与目标主体相符且背景/手部混入可控时，输出目标粗距离。LocateAnything 框包含背景，分区与框重叠不能证明该距离属于目标。必要时提示目标居中、调整观察角度；后续可评估轻量目标掩码，但不默认加重型分割模型。

### 4.3 分阶段引导

1. 查找：按文字描述定位，多个候选需澄清；不播报未经校准的概率值。
2. 锁定：目标稳定并进入相机/ToF 共有视场，测距质量不合格则只给二维方向。
3. 接近：先提示手的左右/上下修正。只有手与目标的深度归属分别可信、同一坐标系转换有效时，才给粗粒度前后建议，不默认二者径向距离之差就是三维间距。
4. 近接触：手遮挡目标、手目标落入同一 ToF 区、距离跳变或目标丢失时，停止数值前后引导，提示放慢、保持、重新观察；不沿用被遮挡前的距离。
5. 确认：手部姿态、物体随手移动和持续跟踪作为辅助证据，最终由用户按键/语音确认。转头可能造成共同图像运动，不能仅凭光流一致就断言拿到。

单个 8×8 ToF 的定位能力存在边界，不能把它插值成可靠的逐像素深度。首版仍采用一颗前向 ToF，先验证“物体方向+粗距离+手部调整”。如果验收要求精确手物间距且实测不满足，再提腕部 ToF 或更高分辨率深度硬件方案，不擅自增加硬件。

拿取模式中的已锁定目标和自己的手不应反复触发普通“前方物体太近”播报；设计短时、范围受限的预期交互区域，非目标风险与失效告警保留。不得简单关闭所有 ToF 告警或长时间屏蔽中心区域。

## 5. 删除盲道/斑马线后的其他功能

- 行进：PP-LiteSeg 候选分割地面/人行区域/车行道/不可通行或未知区域，加 ToF 局部风险。不建立或跟随盲道中心线，不专项检测斑马线。
- 地图：保留目的地与步行路线，遇到道路交叉仍可提示地图事件，但不将其解释为已视觉确认过街安全。
- 红绿灯：保留行人灯状态检测/跟踪与不确定提示。删除斑马线后，不再设计自动“对应斑马线关联/对齐/开始过街”流程。多个方向灯无法确定时明确无法确认，只读灯，不发通行许可。
- OCR：拍照清晰度检查、PaddleOCR、按顺序原文朗读；USB 拍照过程不阻塞测距。
- AI 对话：按需语音/场景问答，不直接决定行进安全；眼镜音频可后续从同一 CDC 通道传 PCM，先用计算端麦克风便于联调。
- 提示优先级：即时风险及失效 > 局部方向 > 地图事件 > 找物/阅读 > 聊天。取消 AI/OCR 播报不等于关闭底层风险监测。
- USB 仅替换眼镜到主机的链路；地图/云端对话可能仍需互联网，不能因此承诺全功能离线。

## 6. 新前端与交互

### 6.1 电脑：工程调试工作台

顶部：USB 未连接/等待握手/已连接/降级、设备标识、模式、帧龄、P95 延迟、ToF 有效率。没有电池传感器时不虚构眼镜电量。

中央：相机画面；按模式切换可通行区域、目标框、手部关键点、ToF 投影、未知区域。叠加结果绑定帧号，过期显示灰态和原因，不把最近一次结果永久显示为当前。

侧栏：当前任务、查找描述/候选物、手部引导阶段、粗距离及可信理由、最近语音事件。

底部：开始/结束任务、取消并静音当前播报、重复提示、拍照、录制/回放。取消播报不能被误解为物理“紧急制动”，系统没有制动执行器。

工程页：USB 吞吐与分片、解析错误、掉帧、模型耗时、ToF 原始网格/状态、标定引导、记录导出。无盲道/斑马线入口、无 SLAM 三维地图。

后端状态是唯一真值；前端发命令带 request_id，显示待确认/成功/失败，避免按钮已变模式而固件未执行。录制默认明确告知且用户触发，图像/语音不自动长期保存或上传。

### 6.2 手机：无障碍操作界面

首页聚焦五项任务：行进/导航、找物拿取、读文字、看信号灯、AI 对话；统一的取消/重复操作位置。大触控区域、清晰标签、TalkBack 焦点顺序，不依赖颜色或视觉画框操作。

插线后显示并语音引导 USB 授权；处理拒绝授权、拔线、重插、App 切后台、屏幕关闭、来电/音频焦点变化。状态改变简短播报，避免每帧朗读检测列表。开发者信息收进独立页面，不让用户面对工程面板。

硬件按键建议一主一取消，手势映射在用户测试后冻结。耳机连接计算端，不额外占用 S3 USB；手机唯一 USB-C 已连眼镜时，优先无线开放式耳机，不默认还能直接插 USB 耳机。

## 7. 硬件与软件清单更新

首选：XIAO ESP32-S3 Sense（随板相机/麦克风）、VL53L5CX 转接板×1、LSM6DSOX 转接板×1、震动及驱动×1、按键×1–2、USB 数据线/必要的 OTG 转接、开放式耳机、刚性相机-ToF 支架与应力释放件。

不额外买 Wi-Fi 模块、USB-UART 视频传输模块、SLAM 相机、第二颗 ToF 或未验证的手机充电分线器。设计级清单仍需核对具体模块电平、上拉、接口占用、峰值电流及近距安装视场。

软件路线：ESP-IDF + esp_tinyusb + esp32-camera + 传感器驱动；Python USB 输入服务 + 可复用 FastAPI 组件；LocateAnything 独立 worker；OpenCV 跟踪、MediaPipe、PP-LiteSeg、PaddleOCR；React/TypeScript 调试前端；后续 Kotlin/Compose + usb-serial-for-android/USB Host。

LocateAnything 权重用途许可与骁龙本地适配仍为未通过关卡。USB 不降低模型计算量，不代表迁移难题已解决；最终不接受把持续连电脑推理当成手机本地目标完成。远端识别和替换模型需要用户另行同意。

## 8. 开发顺序和验收

| 阶段 | 交付与验证 |
| --- | --- |
| Phase 0 工程基础 | 身份/历史检查、Git 基线及远端备份、施工文档、最小目录骨架和基础检查；不实现业务功能 |
| Phase 1 审核 | 权重许可/项目用途、底座可复用性、USB 接口与供电预算、LocateAnything 移动算子风险预审 |
| Phase 2A USB 最小链路 | 相机/ToF/IMU 并行上传、双向命令、30 分钟持续测试、拔插重连、短读和坏包恢复；性能数据不编造 |
| Phase 2B 新前端与回放 | 同属 Phase 2：新工作台、帧号和过期可视化、USB 数据录制/回放、任务取消/确认，不需要先完成所有 AI 模型 |
| Phase 3 行进 | 分割及测距融合，未知/阳光失效/头动降级；确保不加载盲道斑马线工作流 |
| Phase 4 拿取升级 | 标定、LocateAnything 定位、延迟结果对齐、手/目标深度归属、遮挡/多物/失跟与拿取确认 |
| Phase 5 其他辅助 | 红绿灯仅识别、OCR、对话和地图事件；提示优先级/网络异常测试 |
| Phase 6 手机迁移 | 真机 USB Host/供电/后台测试，轻量模型本地运行；LocateAnything 单独给出适配结论，失败则请求决策 |

重点指标：持续有效吞吐、图像采集到反馈的 P95 延迟、传感器时间偏差、过期提示次数、ToF 有效率、风险漏报/误报、找物首次定位时延、目标漂移、错误距离归属、拿取成功与错误确认率。测试集覆盖不同路线/时段和桌面遮挡情况，原型只做受控、有陪同且保留盲杖的测试。

## 9. 本轮主要证据

本轮 Exa 两组定向检索共请求 10 条结果，结合下列官方/项目原始资料复核。以下是设计依据，不是本机性能实测。

- ESP USB 控制器速度及 S3 支持：https://docs.espressif.com/projects/esp-iot-solution/en/latest/usb/usb_overview/usb_overview.html
- USB 设备栈、CDC、PHY 共用：https://docs.espressif.com/projects/esp-idf/en/v6.0/esp32s3/api-reference/peripherals/usb_device.html
- UVC 组件：https://components.espressif.com/components/espressif/usb_device_uvc
- USB 摄像头示例（其吞吐表不能直接当 CDC 或本机承诺）：https://github.com/espressif/esp-iot-solution/tree/master/examples/usb/device/usb_webcam
- Android USB Host/权限/供电：https://developer.android.com/develop/connectivity/usb/host
- Android CDC 开源库：https://github.com/mik3y/usb-serial-for-android
- XIAO 硬件：https://wiki.seeedstudio.com/xiao_esp32s3_getting_started/
- ToF 数据表：https://www.st.com/resource/en/datasheet/vl53l5cx.pdf
- LocateAnything 模型卡及使用限制：https://huggingface.co/nvidia/LocateAnything-3B
- 后端参考底座：https://github.com/AI-FanGe/OpenAIglasses_for_Navigation

版本记录：初稿为方案讨论；2026-09-04 用户授权开始施工，随后要求先修订规范至 V3。本次仅统一现行基线、施工规范和 Phase 编号；不代表依赖已安装、Git 基线已建立或功能已实现。原始设计中的指标仍是预算，待验证；硬件采购仍需具体模块核验。
