# tests

当前仅标准库 unittest 测试基础检查工具的正反例，不是 USB/模型/业务单测。执行 .venv/Scripts/python.exe -m unittest discover -s tests -v。后续按 Phase 增加真实协议/业务测试。

数据边界见 docs/construction/LAYER_CONTRACT.md，实际进度以 HANDOFF 最新记录为准。
