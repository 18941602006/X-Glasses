# 施工进度（追加记录）

## 2026-09-05 / Phase 2B / 开发分支交付 A

A = daa4e91020bb0313829f0529f7d50af07bf92de1，32 文件 3358 增/4 删，已推送 development/continuous-build-20260905 并核验远端一致，A 后工作区干净。main 保持 776a04a6f405b76c6a9d129a3523c3e4264ae640。此追加为审计 B，不预写自身哈希；继续 Phase 3。

## 2026-09-05 / 分支策略临时调整

Phase 2B 批量变更 push main 被平台自动审查在执行前拒绝。为不中断施工且降低共享分支风险，当前成果和后续阶段转入 `development/continuous-build-20260905`；不会用其他命令间接推 main。现行 main/Phase 2B 备份仍保持 B = 776a04a6f405b76c6a9d129a3523c3e4264ae640，最终合并需用户明确批准。

## 2026-09-05 / Phase 2B 前端与本地 API / 本地验收

完成标准库 localhost-only API、离线安全初始快照、有限日志/命令账本、严格动作入口和 uint64 字符串合同；完成 React/TS/Vite 高对比调试台，显示来源/时效/未知、链路/能力、ToF/IMU、标定、命令 ACK 与日志。页面不直连串口、不含盲道/斑马线入口、不复用为 Android UI。

最终 92 项 Python 测试、3 项前端交互、类型检查、生产构建、96 文件范围、Ruff、来源/固件合同、pip/diff 全通过；官方 npm audit 生产依赖 0 漏洞。用户决定省略浏览器截图，视觉/真实屏幕阅读器未人工验收。依赖/测试失败与修复见 LOG；当前准备 A/B 交付。

## 2026-09-05 / Phase 2B 前端与本地 API / 开工计划

基线 B = 776a04a6f405b76c6a9d129a3523c3e4264ae640，拟建立 backup/pre-phase2b-frontend-20260905 并核验。实现 localhost-only Python API 状态快照/安全命令请求层，以及 React/TypeScript/Vite 新调试台；前端不接串口、不直控硬件，不将 pending 当 applied。

界面包含链路与时钟、画面和帧龄、ToF/IMU 有效性、capability、命令 ACK/错误、回放/实机来源、标定占位状态与日志；使用高对比、大点击区、键盘焦点和语义 live region。测试覆盖 API 输入边界/绑定策略/状态序列化及 UI 的未知、断连、过期、pending/rejected 展示和安全停止请求。依赖按官方现行稳定版本与 lockfile 固定；不启动长期外网服务，不记录真实画面。

## 2026-09-05 / Phase 2A 固件协议基线 / 交付 A

A = 47174622f6a42cc818365796bf5851a7156bc9cd，24 文件 757 增/9 删，已 push 指定 origin main，ls-remote 完整哈希一致，A 后工作区干净。86 项测试和静态检查结果见本文件下一条；交付不包含本机编译或实机验收。本追加属于审计 B，不在内容中预写 B 自身哈希。

## 2026-09-05 / Phase 2A 固件协议基线 / 本地验收

实现 ESP-IDF 工程骨架、CDC v1 解析/编码、HELLO/WELCOME、心跳、四时间戳响应、命令参数/截止/session/sequence/去重/ACK，以及主机 capability 强制检查。固件默认只声明 CLOCK，不伪造相机、ToF、IMU、按键或震动已完成。增加 VS Code 团队构建说明和固件合同检查。

用户决定跳过当前施工机的 ESP-IDF 下载与编译，由已有环境的团队成员承担编译、烧录和真机联调。一次本机 export.ps1 失败及停止后续安装如实保留，不能把静态检查写成编译成功。最终软件检查：86 项单测、固件合同、76 文件基础范围、17 来源/6 报告、Ruff、pip、diff 全通过。下一步保存检查点后进入 2B 前端与主机接口。

## 2026-09-05 / Phase 2A 固件 / 连续施工计划

主机第二部分 B = 5201b13b530350f3e9bf5161cb6cfedd24d7894d 已推送核验、工作区干净。本次从该基线创建 backup/pre-phase2a-firmware-20260905-0300 并核验，之后实施 ESP-IDF 5.5.4 / esp_tinyusb 2.2.1 / camera 2.1.7 固件、C 协议/会话/命令去重/心跳/相机分片及传感器驱动接口、有限 MCU 本地告警和固件构建检查。对驱动/工具链按官方固定来源复核后安装到隔离缓存，不改全局环境。

拟改 firmware、tools 构建/协议向量/模拟联调、tests、依赖/硬件/协议文档、基础白名单和施工日志。先编译无板可验部分、跨语言向量和有界/过期/失联逻辑；随后接传感器候选，未接硬件的 I²C/相机/功耗/30 分钟实测单列未验收。端口枚举为空，不刷写未知设备、不采购、不发真实震动。包/工具缓存放被忽略 vendor-cache 或临时目录，精确版本与构建记录入库，依赖产物不入 Git。

新备份只覆盖前两部分已提交资料，当前计划未覆盖；回滚优先明确提交 revert。用户要求连续施工，完成本部分继续新前端和其余独立软件，不因硬件缺失中断。仍分段验证报备并按权限规则执行。

## 2026-09-05 / Phase 2A 第二部分 / 连续施工计划

上一检查点 B = a7bb773726bb6ab5a9a22bfbba9c726ee7ae933a 已 push 核验，工作区干净。用户已明确授权连续全项目施工。本次补齐结构化 ToF/IMU/按键、往返对时与误差/过期、握手/心跳/命令 request_id/ACK/截止和取消的主机控制核心，完善模拟器与测试；随后评估实际 CDC/固件工具链，不提前把硬件未测标通过。

拟改 server/common 消息/时钟合同、server/input 链路控制、tests、tools、USB 文档和当前运行白名单/施工记录。验证合法/未知传感器值、越界/非有限数、旧会话、时钟往返无效/漂移、超时/重复/迟到 ACK、拔线清理及全量回归；不运行模型或访问用户数据。

先从上述 B 建 backup/pre-phase2a-control-20260905-0200 并推送核验，覆盖第一部分已提交成果，本段与层计划暂未覆盖。通过后实施，按检查点审查 A/B 保存；回滚优先明确提交 revert。无需普通选型确认，权限仍按平台规则。

## 2026-09-05 / Phase 2A 第一部分 / 开工计划

用户要求继续。基线 main = 08aaecc722750e59a5009a19bed8d39a099bf08c（Phase 1 审计 B），工作区干净，远端 main 完整哈希一致；身份 Trollhunter / d.o.n.0907@qq.com、SSH 18941602006、指定 origin 均复核通过。

目标：先交付无需硬件/模型的二进制协议、有限组帧与会话清理、流读写适配、原始字节录制/回放和异常测试。此为 Phase 2A 子交付，不是整阶段完成；真实串口适配、对时握手、命令执行/ACK、固件/告警和实机验收后续逐项推进。

拟改 server/common、server/input、tools 回放入口、tests、协议文档；升级基础检查的 Phase 2A 文件白名单，更新入口/架构/进度/日志/交接/MEMORY。只用标准库，不安装重依赖、不下载权重、不启动 UI/App、不访问真实串口或自动录制。

验证：短读/短写、长度/CRC/版本错误、重同步/部分包超时、JPEG 分片/乱序/超限/超时、会话重启及断连清理、回放截断/上限/来源标记；运行全部 unittest、Ruff、pip/结构/来源/diff 检查。所有数据模拟，不替代 30 分钟实机。

备份计划：从该 B 建唯一 backup/pre-phase2a-protocol-20260905-0110，push 并核验后实施；只覆盖已提交历史，两份本轮计划不在备份中。保留 main，回滚优先对明确提交 revert，不覆盖用户文件。收尾交付 A/审计 B。

## 2026-09-04 23:47 +08:00 / 规范对齐 / 开工计划

### 本轮目标

按用户最新要求，将旧《X-Glasses 施工提示词.txt》修订为适配方案 V3 的唯一现行工程规范。只改文档，暂停此前 Phase 0 初始化操作。

### 涉及层

产品与施工规范、阶段映射、历史记录；不涉及真实固件、服务或前端代码。

### 当前仓库状态

当前目录仅有方案 V2、方案 V3、MEMORY.md；尚非 Git 仓库。上轮已通过 SSH 认证，账号为 18941602006；尚未检查远端历史或建立备份。当前命令环境找不到 python，Git user.name/user.email 查询无值，需后续处理，不能编造身份。

### 计划修改

- 新建根目录《X-Glasses施工规范V3.md》，保留外部原文不覆盖。
- 修订《方案V3.md》版本状态及阶段编号，统一 Phase 0–6，Phase 2 分 USB 链路和调试前端两段。
- 明确删除盲道/斑马线、采用 USB、ESP-IDF、手部融合、前端重设计、MCU 基础告警例外及 Git 审计收尾规则。
- 建立本次规范修订日志与交接，更新 MEMORY.md，记录上轮中断前真实检查结果。

### 测试计划

文本一致性、禁用旧条款、关键规则覆盖、文件可读与实际文件清单检查；没有业务测试，不声称 lint/单测或 Phase 0 通过。

### GitHub 备份计划

本轮是用户要求的规范修订和计划记录，未进入须备份后才能进行的真实施工；不初始化、commit 或 push。后续 Phase 0 的 Bootstrap 和备份流程以新规范执行。

### 回滚预案

原外部规范和方案 V2 保留；本轮只做已列明文档变更，不删除用户文件。尚无可引用的 Git 回滚提交。

## 2026-09-04 / 规范对齐 / 收尾

- 规范修订完成：X-Glasses施工规范V3.md 已替代旧模板的现行地位；方案 V3 和阶段编号已同步。
- 10 项文本一致性检查通过，文件可读；无代码测试、lint 或 Git 提交。
- LOG/HANDOFF/MEMORY 已更新。Phase 0 仍停在前置检查，不标阶段完成。
- 恢复时先处理提交身份、Python 路径、远端历史与备份门槛。

## 2026-09-04 23:58 +08:00 / Phase 0 恢复 / 前置计划与停止点

用户明确授权开始施工，要求每完成一部分报备。施工顺序：环境/远端核查 → 身份确认与最小 Bootstrap → 准备文档基线和远端备份 → Phase 0 文档/最小骨架 → venv/基础检查 → 提交推送与交接。

- 涉及层：00-foundation；本轮停在前置检查，尚未进入基础骨架施工。
- 当前 Git 状态：本地不是 Git 仓库，无分支/HEAD/origin；SSH ls-remote 成功且无 refs，未核实写权限。Git user.name/user.email 仍缺失。
- 拟改文件：当前仅 DEV_PROGRESS、LOG、HANDOFF、MEMORY 及 00-foundation 层计划；身份和备份通过后再建立规范列出的文档、README、AGENTS、.gitignore、.env.example、最小检查配置和层说明。
- 验证计划：前置阶段检查解释器/提交身份/远端；施工后才执行 venv、最小锁定依赖的 lint、git diff --check、文档一致性；有测试则执行，无测试如实记录。
- 备份计划：身份经确认后只设仓库级配置；空远端走最小 Bootstrap。审查准备资料及哈希，形成明确文档基线后创建并 push 唯一 backup/pre-phase0-foundation-<yyyymmdd-hhmm>，核验覆盖范围，切回 main 再施工。
- 回滚预案：当前未修改用户业务文件、未初始化或推送，无 Git 回滚点；后续基于已核验提交优先 revert，不覆盖未知改动。
- 已完成部分：环境与远端只读核查，Python 3.12.14 可执行；远端读取成功。
- 当前阻塞：需用户指定 Git 提交姓名，并确认邮箱 d.o.n.0907@qq.com。按规范第 5/13 节停在提交/备份之前，不以“后面再问”跳过。
- 尚未完成：Bootstrap、远端备份、工程骨架、venv/lint/单测；Phase 0 未通过。

## 2026-09-05 / Phase 0 / 身份确认后开工计划

用户指定提交姓名 Trollhunter 并要求继续；邮箱沿用已指定的 d.o.n.0907@qq.com，仅设仓库级配置。已复核 SSH 账号 18941602006，指定远端仍无 refs，本地仅有来源明确的项目文档。

本轮目标与层：完成 00-foundation，不实施业务功能。先最小 README/.gitignore Bootstrap，审查并提交准备文档基线，创建唯一远端备份并核验；再建立规范第 14 节文档、各层职责说明、空值 .env.example、最小锁定 lint 依赖、基础检查工具和测试。计划验证文档路径/合同、忽略规则、lint、工具自身测试、diff --check、远端哈希及工作树；失败记录并复测。回滚以备份基线和明确提交 revert 为依据，不覆盖用户历史。

当前尚无本地 HEAD/备份。拟提交的准备资料只有方案 V2/V3、规范 V3、MEMORY、已有施工记录；先记录清单与 SHA256，检查无密钥/真实配置/数据。备份只覆盖已提交资料，未提交部分另记。最终通过 A 交付、B 审计收尾。

## 2026-09-05 / Phase 0 / 本地验收完成，准备交付

- 身份：本仓库 Trollhunter / d.o.n.0907@qq.com；SSH 18941602006，origin 正确，未改全局配置。
- Bootstrap fb23e6fbd1b822db85160e5f641af9b0bff9b02d；准备资料基线 76bb6d685a02a833056515350cd0a4eccee5d4fc。
- 已核验远端备份 backup/pre-phase0-foundation-20260905-0006 与准备基线一致，main 推送成功；仅在 main 施工。
- 完成：规范要求的文档与 00–06 层记录、各层 README、AGENTS、空配置、忽略/换行规则、最小依赖清单、可执行基础检查工具及其单测。
- 环境：Python 3.12.14 项目 .venv；Ruff 0.12.12；无模型/固件/前端依赖和业务实现。
- 复测：pip check、Ruff lint/format、45 必需文件/空配置/忽略/目录检查、13 项工具单测、git diff --check 全通过。首轮格式失败及执行器错误见 LOG。
- 文档漂移：身份和历史阻塞已更新，仍为 V3/USB/无 SLAM 与盲道斑马线专项/手部融合/新前端；后续层明确未开始。
- 待办：提交并核验 A，追加审计 B 后核验；此前不标完整远端交付。Phase 1 下一阶段审核，不下载受限权重。

## 2026-09-05 / Phase 0 / 交付验收

Phase 0 工程基础已完成：交付 A = 66dc754444148a818beb5a06af74f6ec2f0902b4，main push 成功且远端哈希一致，推送后工作区干净。此后本次修改仅补审计 B，B 推送/最终工作区状态以终端核验为准。下一阶段 Phase 1 尚未开始，不把工程基础视为产品功能完成。

## 2026-09-05 / Phase 1 / 开工计划

用户要求继续下一步。本地 main = 34fcde23e9bfa9161ff2c4175bd7c2104ac7cfb3（上轮审计 B），开工检查工作区干净，身份/SSH origin 正确。

目标：01-deps-license，按官方源码/清单/文档审核底座复用范围、准确版本/commit、代码/模型许可、USB/硬件接口及手机风险。使用 Search 技能分开核查开源底座、模型部署、USB/硬件三组；只读原始资料，不运行第三方脚本或下载模型权重。

拟修改：docs/dependencies 下来源锁定和许可/复用台账、docs/hardware 接口及供电核查、docs/android-migration 风险；必要时增加审核清单验证工具和测试，更新 AGENTS/README/架构及日志/层进度/HANDOFF/MEMORY。不导入未经许可的业务代码，不采购、不构建 App。

备份计划：backup/pre-phase1-audit-20260905-0026，从上轮审计 B 创建并 push/核验；覆盖已提交 Phase 0，全程 main。当前两份开工计划未纳入此备份，交付时另行提交。回滚优先明确提交 revert，无破坏性操作。

验证：来源 URL 与精确引用/许可证交叉核验，标注已审查/候选/阻塞/未实测；复跑基础 lint/format/结构/单测/diff，新增台账则验证 schema 和状态约束。阶段审核交付不等于依赖全部运行验证；许可用途或硬件信息不足单独记录，不编造通过。

## 2026-09-05 / Phase 1 / 审核成果与本地验证

已完成来源与风险审核：17 条精确来源/版本快照、底座逐文件复用边界、隔离环境/许可差异、硬件接口/供电、Android 风险。新增审核工具与 10 项测试；全部 23 工具单测、基础 45 文件检查、17 来源/6 报告检查、pip check、lint/format、diff --check 通过。首轮格式失败已修复复测。

备份已核验为 34fcde23e9bfa9161ff2c4175bd7c2104ac7cfb3。尚无本轮 A/B；待审查提交推送。阶段为“审核交付，保留实施关卡”，不宣布完整运行依赖/权重许可/硬件适配完成。下一步可在独立计划和备份后做 Phase 2A 不依赖模型的 USB 协议/输入；权重使用前须确认用途，具体模块上电前须核查电平/功耗。

### 用户追加用途确认（2026-09-05）

用户明确 LocateAnything 仅测试、非学术/非盈利；按固定 LICENSE 的评测范围将状态改 evaluation_only，不再重复询问用途。保留许可说明差异及远程代码/第三方条款/环境/实机关卡。新增两项授权边界测试，全部 25 测试通过；尚未运行模型或下载权重，按既定阶段继续。

### 审核交付核验

A = fb9d88a18ed6efe9acbdca5b397754fe9e43c3c3，已推送并核验远端 main 一致，推送后工作区干净。Phase 1 审核成果交付；剩余实施关卡保留。此追加为审计 B，最终 B 状态由终端核验。下一步 Phase 2A USB 软件，不以审核替代实际功能验证。

## 2026-09-05 / Phase 2A 第一部分 / 实施与用户持续授权

已建立包头/CRC/有界解析、JPEG 组帧/单次消费、显式会话清理/流读写接口、XGR1 原始录制与虚拟时间回放，合同 docs/protocol/USB_V1.md。无新运行依赖、模型、固件/串口/UI。原 25 测试加协议/回放/范围测试合计 57 项，完整复测以 LOG 为准；尚待本次 A/B 收尾，不能预写推送成功。

用户追加要求连续施工直到完整项目，不在中间等待一般技术选择。后续按阶段连续执行、小检查点保存并报备，不为询问普通选型停工；优先完成所有可在现有环境实现/验证的软件。此授权不等于可绕过平台权限、不自动授权采购/付费/隐私数据上传，不把无硬件/手机实测的阶段标已验收。若硬件阻塞仅影响实测，记录未验收项并继续不依赖该实测的工程部分，不以占位模块宣称功能完成。


## 2026-09-05 / Phase 2A 协议核心 / 交付 A 核验

A = 31577ff742ca0cd66cd8f0828b9af9ef0ac71009，27 文件 1187 增/17 删（状态摘要调整，历史保留），已 push 指定 origin main，ls-remote 完整哈希一致，A 后工作区干净。最终 Ruff check/format（11 文件）、53 必需文件、17 来源台账、57 单测（约 1.257 秒）、纯内存 demo、pip check、diff/cached diff 均通过。明确暂存清单、小于 1MiB 和常见凭据模式检查通过；这不是通用秘密审计或实机认证。

此追加为审计 B，B 不记录自身哈希，终端核验后继续下一子任务；不在此停止等待用户。下一步仍 Phase 2A 会话/传感器语义/对时/ACK，完整实机链路未验收。备份 backup/pre-phase2a-protocol-20260905-0110 仍指向 08aaecc722750e59a5009a19bed8d39a099bf08c。


## 2026-09-05 / Phase 2A 第二部分 / 连续施工记录

基线 B = a7bb773726bb6ab5a9a22bfbba9c726ee7ae933a 已核验；先写计划并 push backup/pre-phase2a-control-20260905-0200，远端哈希同值，再实施。两份开工计划未包含在该备份中。main/身份/指定 origin 不变，用户持续施工授权有效，不等重复确认。

新增 common/{sensors,clock,control}.py、input/{link,serial_port}.py，CONTROL_V1 合同、可选 requirements-input.txt、usb_probe CLI 和测试。归一化未知 ToF= None，IMU 非有限值拒绝；握手/心跳清理、四时间戳/误差/5s 过期/跳变清估计、命令 request/session/ACK/500ms 截止，所有队列有界。原 InputSession 回放保持未对时，HostLink 只输出新鲜估计帧并携带误差，不等于标定或安全认证。

pyserial 3.5 官方 wheel SHA256 固定并安装到项目 venv，内存 loop:// 回环测试通过；只读枚举端口结果 []，未打开实际设备。官方 v3.5 BSD-3-Clause 与 wheel 源码 SPDX 已核对，wheel 无独立许可证文件；运行台账 docs/dependencies/INPUT_RUNTIME.md，Phase 1 快照保留历史。Node 22.17.1/npm10.9.2、RTX4090 Laptop 16376MiB/驱动595.79 已读；PATH/常见目录未发现 JDK/IDF/Android SDK，不断言全盘不存在。

验证：控制首轮 18 项与串口 2 项通过；全量 77 项、64 必需文件/9 运行白名单、17 来源、Ruff check/format（19 文件）、pip check 通过；追加时钟跳变回归后控制 19 项通过，最终全量现为 78 项，收尾复测另记。格式化前自动修正新测试 1 处导入排序；时钟保护补丁一次上下文不匹配、未落盘，读实际行后成功重试。许可证先猜 LICENSE.txt/rst 不存在，rg 实际枚举后改查 metadata/SPDX 与官方原文，无依赖代码修改。代码审查另修复 idle 重同步丢包、结果缺会话标签和缺对时误差字段。

当前本部分 A/B 尚待提交，不预写 push/干净状态；无需回滚、无文件删除、无长期采集进程。阶段未放行实机：固件、晶振预算、传感器归一化、实际 USB/供电/30 分钟链路都未验证。下一步保存此检查点后继续 ESP-IDF 固件与跨语言协议/编译，不因无硬件停止独立软件工作。


### 第二部分交付 A 审计

A = 2c45a4839d3fd266d6bb899995601db8d0f15290，30 文件 1013 增/10 删，已 push 指定 main 且 ls-remote 完整哈希一致，A 后工作区干净。最终 78 测试（约1.532秒）、64 文件、17 来源、Ruff19文件、pip/diff/cached diff 通过，显式文件体积及常见凭据模式检查无命中。第二部分软件检查点通过，实机仍未验收。本追加为 B，自身哈希终端核验后继续固件，不停等用户。
