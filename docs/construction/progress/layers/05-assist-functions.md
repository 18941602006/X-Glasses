# Phase 5 / 辅助

## 2026-09-05 / 开工计划

基线 9455b7c783fefcb1f265cde659456107384689b4；完成备份后实施供应商无关的信号灯、地图、OCR、对话事件适配合同和唯一输出仲裁。输入长度、响应字段、超时、session/model/provider 回显及队列均设上限；默认离线，不读取真实凭据。

信号灯事件只能报告 red/green/unknown 和方向是否明确，禁止给过街许可；地图仅为路线提示；OCR 保留原文字段与阅读顺序；对话永不参与障碍决策。风险/失效事件必须抢占并取消较低优先级输出，合成测试不替代模型和道路实测。

## 2026-09-05 / 软件核心验收

已实现 `assist/contracts.py`、隔离 `worker.py`、四类辅助事件解析、道路/拿取/辅助事件到输出候选的显式映射、固定八级仲裁和 deadline-aware 输出执行边界。外部 JSON 必须回显 session/correlation/model/provider；JPEG、问题、响应字段、文本、OCR 行数、队列类别和超时均有上限。

绿灯文字固定包含“不是过街许可”，仲裁自由文本再次拒绝常见中英文过街授权短语；感知未知、紧急停止和局部方向依次抢占信号灯、地图、拿取/阅读及对话。过期/未来/错会话拒绝，同类旧事件不得覆盖新事件，取消、换会话清理、重复限频及执行回执均有合成测试。

阶段新增 23 项测试，全仓 139 项通过；117 文件范围、来源台账、compileall、Ruff check/format 和 diff 检查通过。状态为软件合同完成，实际灯/OCR/对话模型、地图 provider、TTS/震动驱动、联网/断网和真实场景均未验收。

## 2026-09-05 / 阶段登记

状态：软件合同与仲裁核心完成；供应商、模型、真实输出和实机场景待验收。

后续范围与验收：OCR、灯状态、地图事件、对话，验证错误/网络异常/取消/风险抢占；不发过街许可。地图服务地区/对话供应商等按需确认，不提前配置真实密钥。

开工时补充当前分支/基线、拟改文件、测试命令、成功远端备份和回滚方案后再施工；不得将此登记当作已完成。
