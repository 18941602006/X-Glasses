# Phase 4 / 找物拿取

## 2026-09-05 / 开工计划

从开发分支 66eeb8ab8d95f1b8157260730067eed3b0b4a566 建备份，先实现 LocateAnything worker 结果合同与手/物/ToF 保守融合。目标定位仅 evaluation_only；同区/遮挡/不同步时不报前后距离，手物重叠只能提示用户确认，不能自动宣告拿取完成。

## 2026-09-05 / 软件核心验收

已实现模型隔离合同、归一化目标框、手部观测、标定 ToF 分区关联、三轴有限期引导和一次性显式确认门。worker 必须回显固定 session/frame/model，输入 JPEG、查询和目标数量均有上限；时间、标定、质量、遮挡、无效距离和同 ToF 区域任一不满足时均降级。

ToF 网格边界采用确定性的左闭右开归属；手物二维重叠只输出 confirm 请求，只有有效期内的 button/voice 确认可完成，其他来源及重复确认均拒绝。新增 9 项合成测试，Python 全量 116 项通过（1 项可选 pyserial 跳过），基础结构 110 项和 compileall 通过。

本阶段只完成可复测的软件安全合同。LocateAnything 权重、worker、手部模型、相机/ToF 外参标定、目标跟踪及真实拿取成功率仍未验收；不能将合成框与距离测试表述为实物识别或抓取完成。

## 2026-09-05 / 阶段登记

状态：软件核心已实现并通过合成回归；模型运行、标定和实机验收待团队环境与硬件。

后续范围与验收：LocateAnything + 跟踪 + 手部/ToF：标定、晚到结果传播、距离归属、共区/遮挡降级、多物澄清和用户确认。用途已确认为非商业测试；权重来源与隔离环境未验前仍不下载，不能仅凭握拳判成功。

开工时补充当前分支/基线、拟改文件、测试命令、成功远端备份和回滚方案后再施工；不得将此登记当作已完成。
