# X-Glasses 工程施工规范 V3（现行定稿）

版本：3.0。修订日期：2026-09-04。

用途：替代原《X-Glasses 施工提示词.txt》作为后续施工执行规范，配套根目录《方案V3.md》。原文及方案 V2 仅保留作历史，不再作为现行技术总纲。定稿的是施工规则与设计基线，不代表模型性能、硬件兼容或许可用途已经验证。

本文档不因被读取而自动触发施工、push 或进入下一 Phase。按用户当前明确授权和 HANDOFF 记载推进；用户要求修订文档时，不执行旧模板中的“立即开始 Phase 0”。

## 一、项目身份与优先级

- 项目名称：X-Glasses。
- 项目负责人/管理员：Trollhunter。
- GitHub 用户：18941602006。
- 指定仓库：18941602006/X-Glasses。
- 指定 origin：git@github.com:18941602006/X-Glasses.git。
- 指定 Git 提交邮箱：d.o.n.0907@qq.com。
- 不将管理员姓名自动作为 Git user.name，不沿用原模板其他身份。
- 每次回复以“报告长官！！”开头；每轮更新 MEMORY.md，不能用它替代施工 LOG/HANDOFF。

在遵守运行环境权限和安全规则的前提下，项目决策优先级为：

1. 用户最新明确指令。
2. 方案 V3 的功能范围、技术决策和安全边界。
3. 本施工规范 V3。
4. docs/construction/CODEX_MASTER_REQUIREMENTS.md。
5. docs/product/PRODUCT_REQUIREMENTS.md。
6. 其他施工文档与已记录架构决策。
7. 旧代码、历史规范及 Agent 偏好。

下级文档不得恢复已经删除的需求。有冲突先按上级规定修正文档并在 LOG.md 记录；涉及关键用途、身份或授权的冲突不能自行猜测。

## 二、现行产品范围

眼镜由 XIAO ESP32-S3 Sense、前向 VL53L5CX 多区 ToF、LSM6DSOX 六轴 IMU、按键及震动反馈构成。图像和传感数据经 USB 数据线传至电脑，后续迁移至骁龙 8 系列 Android 手机。

保留：道路/候选步行区域分割、近距避障、LocateAnything 找物与拿取辅助、行人信号灯状态识别、地图步行导航、AI 对话、OCR。

明确排除：

- 盲道专项检测、沿盲道中心线导航。
- 斑马线检测、对齐、关联通行与自动过街工作流。
- 上述功能的模型加载、状态、接口、页面入口和语音命令；不得借“对照模块”恢复到运行系统。
- SLAM、三维建图、回环、室内自主定位。
- 默认远端 LocateAnything、未经同意的替代识别模型。

删除专项识别不等于删除道路语义分割。红绿灯仅报告状态，方向/对象不明确时提示无法确认，不自动判断应遵从哪一个信号灯。

## 三、安全边界与不确定性

- 不输出安全过街、脚下无落差等保证；受控、有陪同、保留盲杖，不安排独立道路/过街试用。
- ToF 无回波或质量不足标记未知，不当作空旷。单前向 ToF 不承诺覆盖脚下落差。
- 车行道不默认标记为可走；缺少尺度时不断言通道宽度足够。
- 头部朝向不等于身体行进方向；IMU 不独立提供可靠绝对航向。
- LocateAnything 输出二维框/点，不自动提供深度、物体掩码或拿取动作。
- 手物二维重叠、握拳或共同图像运动不等于成功拿取；必须保留用户确认。
- 云端对话不参与实时风险决策；取消聊天不关闭基础风险监测。
- USB 仅替代眼镜通信链路，不使在线地图和云端对话变成离线功能。
- LocateAnything 权重用途许可必须在使用前确认；源码许可不能代替权重许可。手机本地适配未通过不得宣称最终目标完成。
- 帧率、延迟、量程、续航属于待验证指标，不能把设计预算写成实测。

## 四、开工检查与停止条件

先确认目录和原有文件，再判断是否可改动。PowerShell 使用等价原生命令，不照抄 Unix 语法，不更改用户全局配置。

只读检查至少覆盖：

- 当前绝对路径、隐藏文件、限定深度文件列表或 rg --files。
- Python/Git 版本及实际可执行路径。
- git rev-parse --is-inside-work-tree、branch --show-current、status --short、remote -v。
- git config --get user.name 和 user.email。
- 存在 HEAD 时记录完整哈希和最近提交。
- 现有 AGENTS.md、施工文档及依赖文件；存在应用则先识别技术栈，不重建覆盖。

没有 Git 仓库属于环境事实，不自动代表报错需修复。目录为空或仅有来源明确的本项目准备文档可以进入安全初始化流程；来源不明文件可能冲突时停止。

以下情况必须停在安全边界并报告：目录错误、可能覆盖未知用户改动、SSH 身份不符、远端/历史冲突、提交身份缺失/不符、必要权限缺失、必须执行破坏性操作或其他关键阻塞。一般设计细节可先记录后续处理，不因此扩大授权。

禁止默认执行 reset --hard、clean -fd/-fx、checkout --、restore 覆盖、force push、合并无关历史或删除仓库。不得静默把未知改动纳入提交。

## 五、SSH 和 Git 身份

- 只使用指定 SSH 地址，不退回 HTTPS，不连接其他仓库代替。
- 执行 ssh -T git@github.com，检查成功输出中的账号严格为 18941602006。成功认证可能返回退出码 1，结合输出判断。
- 禁止读取、打印、复制 SSH 私钥；主机密钥不得通过关闭验证绕过。新主机密钥验证或网络权限问题使用受控授权流程。
- 记录实际使用的 ssh/git 客户端；Windows 可用 Git Bash 复核，若改用原生 OpenSSH，记录环境差异，不为换 shell 修改密钥配置。
- user.name/user.email 已存在且符合要求则保留；邮箱不符报告，不擅改。
- 缺失身份时第一次 commit 前停止并请求用户指定/确认；不以 Trollhunter 或 GitHub 用户名推测提交姓名。经同意只设置仓库级配置，不用 --global。
- 已有环境变量形式的提交身份也要核实来源并明确记录，不隐瞒覆盖配置的行为。

## 六、Remote 与历史初始化

origin 无配置时添加指定 SSH 地址；若为同一仓库 HTTPS 地址可切为指定 SSH；已正确则不修改。指向其他仓库时不得直接覆盖。

非 Git 目录可先用指定完整 SSH 地址执行 ls-remote 检查远端；不必先创建无关本地历史。核对 ls-remote 退出状态，空输出可能表示空仓库。

| 状态 | 处理 |
| --- | --- |
| 本地空目录，远端有历史 | 克隆指定仓库，不建立无关历史 |
| 本地仅有本项目准备文档，远端有历史 | 先在安全临时目录检查远端文件/历史，再规划无覆盖的接入；不盲目 clone 到非空目录，不覆盖同名准备文档 |
| 本地无历史，远端确认为空 | 允许一次性 Bootstrap：git init、main、指定 origin、最小 README/.gitignore |
| 本地有历史，远端为空 | 审核历史和提交内容后保留历史，确认分支命名不冲突再推送 main |
| 两边有历史 | fetch 后检查共同祖先、领先/落后/分叉；分歧先分析，禁止强推或合并无关历史 |

Bootstrap 不含固件、服务、模型或重依赖。提交前必须通过身份检查、diff --check 和内容审查。仅显式列出需要的文件暂存。

对已有方案/记忆等准备文档记录文件清单与哈希；如需纳入备份，先检查公开推送适宜性并形成明确的文档基线提交。备份分支只能保护已提交内容，未提交/未跟踪文件必须单独列明为“未被远端备份覆盖”。不能把只含 README/.gitignore 的备份描述为覆盖所有准备资料。

## 七、开工计划、远端备份及允许的前置文档

每轮真实施工前在 DEV_PROGRESS.md 和当前层进度文件追加计划：目标、层、实际 Git 状态、拟改文件、验证、备份和回滚方案。文档尚未建立时，可先创建这些计划记录；第一条记录应为开工计划。

以下属于受限前置操作，可在开发前备份前进行：安全检查、用户要求的规范修订、计划/阻塞日志与交接、空仓库必要的最小 Bootstrap。它们不代表真实功能施工或 Phase 完成。

在安全提交基线上创建唯一备份分支：

`backup/pre-phase<N>-<scope>-<yyyymmdd-hhmm>`

将备份 push 到指定 origin，核实成功后切回 main，记录基线哈希、备份引用和覆盖范围。备份 push 失败，禁止继续真实施工；不在备份分支开发，不复用已有备份名称。

## 八、现行技术栈

| 层 | 选型/边界 |
| --- | --- |
| 固件（Phase 2 起） | ESP-IDF + esp_tinyusb + esp32-camera + ToF/IMU 驱动；不采用旧规范默认 Arduino-ESP32 框架。版本在 Phase 1 审核锁定 |
| USB | S3 原生 CDC-ACM + 二进制分包；电脑/手机为 Host，S3 为 Device，不经 USB-UART 桥传视频 |
| 电脑（Phase 1/2 起） | Python venv；可复用 FastAPI 后端模块；主机接收层与算法解耦 |
| LocateAnything | NVlabs/Eagle/Embodied、nvidia/LocateAnything-3B；独立进程/环境，使用前完成许可审核 |
| 感知 | PP-LiteSeg 候选、专用交通灯模型、PaddleOCR、MediaPipe、轻量单目标跟踪 |
| 电脑前端（Phase 2B 起） | React + TypeScript + Vite；重新设计调试台，不启动旧盲道页面 |
| Android（Phase 6 起） | Kotlin + Compose + USB Host/usb-serial-for-android；此前仅风险预审，不创建 App 工程 |

Python 基础工程目标 3.11+，但模型环境按官方兼容范围分别锁定，不强求一个解释器覆盖全部框架。找不到 Python 时优先定位已有合适解释器，不全局安装/覆盖环境；必要安装按权限流程执行。

依赖精确版本和来源记录到对应清单/锁文件；不能只用“3.11+”或浮动版本声称可复现。Phase 0 只装最小 lint/测试依赖，不装 PyTorch、Transformers、Paddle、MediaPipe、固件工具链、前端工程依赖，不下载模型或数据。

## 九、USB 与供电合同

- S3 为 USB Full-Speed，不能规划成 High-Speed 摄像头；测 JPEG 最坏帧长、持续吞吐、端到端延迟。
- 协议带版本、session_id、类型、序号、采样时间、长度、校验；图像分片带 frame_id/总长/偏移；所有缓冲有上限。
- JPEG 分片之间允许插入高优先级消息，不往 USB 栈一次塞入大量图像造成队头阻塞。
- 处理短读/短写、超时、坏包重同步与重要命令确认；丢弃旧图像，不反复重传过期帧。
- 单线不等于同步采样；按设备采样时间配对和估计时钟偏差，不用收到数据的时间冒充曝光时间。
- CDC 图像通道不夹杂裸日志；USB-OTG 与 Serial-JTAG 共用 PHY，预留下载模式恢复，不烧 eFuse 绕过限制。
- localhost HTTP/WebSocket 仅服务电脑内部前后端，不违反眼镜 USB 有线要求。
- 同时只连接一个计算主机；原型先 USB 供电，验证总电流、峰值和主机限制。
- 眼镜断电后无法本地告警，拔线提示由仍有电的主机发出；清除旧引导。禁止承诺无电继续工作。
- 电池、PD Hub、UVC 或 vendor bulk 作为后续经评估的变更，不私自加线并联供电、不默认启用。

## 十、分层与依赖边界

| 位置 | 职责 |
| --- | --- |
| firmware/ | 采集、USB 打包、按键/输出驱动、链路状态和下述有限基础告警 |
| server/input/ | USB 接入、组帧、时间对齐、有效性/断连事件 |
| server/common/ | 公共协议、数据结构、时钟与状态契约；不依赖具体感知模型 |
| server/perception/navigation/ | 道路分割、ToF 风险融合、短时局部方向；不含盲道/斑马线模型 |
| server/perception/locate/ | LocateAnything 适配、跟踪、手部与 ToF 融合、拿取状态 |
| server/perception/assist/ | 交通灯仅识别、OCR、对话及地图事件 |
| server/arbitration/ | 只接受结构化事件，仲裁优先级/有效期/节流，不读取相机帧或加载模型 |
| server/output/ | 接收仲裁结果，执行 TTS/音频及向固件发震动命令 |
| frontend/ | 电脑调试前端，显示主机确认状态，不能绕开仲裁直接控制硬件 |
| tools/ | 标定/回放/评测，通过公开数据契约或适配接口调用，不导入模型私有实现 |
| docs/android-migration/ | Phase 6 前仅风险预审和迁移合同 |

感知模块不得直接操作扬声器/震动；LocateAnything 阻塞不得传到实时风险链路。跟踪只延续已识别目标，不能替代指定识别模型。第三方代码保留来源 commit、许可证和署名。

### MCU 基础告警例外（消除原规范内部矛盾）

固件不运行视觉模型，不做语义识别、手物融合、路线规划或通行决策。但允许独立、有限的 ToF 有效近距阈值提示及数据失效提示，前提是 ESP32 仍有电；其阈值/滞回/限频与适用视场需要测试并记录。

该本地事件回报主机，主机避免重复播报；固件内统一输出调度解决本地与主机命令冲突。此例外不赋予普通主机感知模块绕过仲裁的权限。拿取模式只能短时限制预期交互区域的重复提示，失效及其他风险不得整体屏蔽。

## 十一、手部融合升级合同

- 标定相机内参/畸变、ToF 分区视场及外参、时间偏差和图像裁剪映射。
- LocateAnything 返回源 frame_id/时间和查询；晚到结果可靠传播到当前帧，否则重定位，不能直接覆盖当前画面。
- 目标框含背景，多区 ToF 不是逐像素深度；仅在目标归属可信时播报粗距离。
- 手/目标落入同一格、遮挡、距离跳变、失跟时降级；禁止沿用旧距离指导伸手。
- MediaPipe 相对深度不能直接与 ToF 相减；径向距离差不等于三维手物间距。
- 流程：查找 → 澄清/锁定 → 跟踪 → 二维手部调整 → 可信时粗前后提示 → 近接触降级 → 多证据辅助与用户确认。
- 一颗前向 ToF 先验证，不默认增加腕部传感器或声称厘米级拿取。
- 记录目标漂移、错误距离归属、首次定位延迟、拿取成功和错误确认率，而非只报 FPS。

## 十二、新前端合同

电脑工作台：USB 状态、设备/模式、帧龄/P95 延迟/ToF 有效率；视频上按模式显示分割、目标、手部、ToF 投影；任务/拿取阶段、日志/回放与标定页。过期叠加灰化且标原因，无盲道/斑马线页面、无 SLAM 地图。

用户操作带 request_id，只有后端确认才显示成功。提供开始/结束、取消当前播报、重复、拍照、录制；不得把软件取消标成设备具有物理制动能力。

手机界面独立设计：行进导航、找物拿取、读文字、看信号灯、AI 对话五项入口；大触控、TalkBack、有序焦点、状态变化语音提示，处理 USB 授权/拒绝/重连、后台、来电和音频焦点。不把工程调试信息强加给用户。

共享协议与业务状态，不承诺桌面 React 和 Android Compose 共用 UI 代码；录制和云端上传需明确用户触发，不虚构设备没有采集的电量等指标。

## 十三、统一 Phase 0–6

唯一正式编号如下；方案 V3 旧 P 编号不再用于施工日志。Phase 2A/2B 是同一阶段子项。

| Phase | 内容 | 阶段验收 |
| --- | --- | --- |
| 0 Foundation | SSH/身份/历史、安全基线与备份、施工文档、层目录说明、venv 和最小检查工具 | 基础检查真实执行、文档无漂移、备份和交付 push 核实；无业务实现 |
| 1 依赖许可 | 开源底座审核锁定、LocateAnything 许可及隔离、USB/框架兼容、移动风险预审 | 来源/版本/许可证/阻塞清单；不得未确认许可先下载权重 |
| 2A USB 链路 | 固件、统一输入、时间对齐、双向命令、基础告警、回放格式 | 编译、协议测试、30 分钟实机链路、拔插与供电/吞吐记录；无硬件时明确哪些未测 |
| 2B 调试前端 | 新工作台、帧号/过期状态、回放、标定入口和控制确认 | 构建/交互测试；模拟和实机数据明确区分 |
| 3 核心行进 | 道路分割和 ToF 局部风险，无盲道/斑马线 | 回放及受控实测漏报/误报/延迟、未知状态与光照失效降级 |
| 4 找物拿取 | LocateAnything、跟踪、手部-ToF 融合与确认 | 多物/无物/遮挡/晚到结果/错误归属/拿取确认测试 |
| 5 辅助功能 | OCR、看灯、对话、地图事件与提示抢占 | 接口/回放/异常测试，不输出安全过街承诺 |
| 6 Android | USB Host、轻量模型及任务迁移、LocateAnything 专项适配 | 一致性、后台/功耗/内存/温升/时延；本地目标未达成则不宣布完成 |

按阶段检查点推进，不越过当前阶段大量开发后续功能。一般问题记录后续处理；身份/权限/数据安全/许可等硬门槛不能以“后面再问”为由跳过。

## 十四、Phase 0 文档和最小骨架

建立根 AGENTS.md，以及下列施工文档（未建立时先按前置计划规则创建，不能假装已读不存在的文件）：

- docs/product/PRODUCT_REQUIREMENTS.md。
- docs/construction/CODEX_START_HERE.md、CODEX_MASTER_REQUIREMENTS.md、ARCHITECTURE.md、CONSTRUCTION_PLAN.md、DEV_PROGRESS.md、LOG.md、HANDOFF.md、WORKFLOW.md、GITHUB_ROLLBACK.md、TEST_METRICS.md、LAYER_CONTRACT.md、TOOL_POLICY.md。
- docs/construction/progress/layers/00-foundation.md、01-deps-license.md、02-glasses-link.md、03-core-navigation.md、04-locate-grasp.md、05-assist-functions.md、06-android-migration.md；02 中记录 2A/2B。

AGENTS.md 只列身份、优先级、必读順序、当前 Phase、开工收尾及禁令，不复制整份需求。

每次开工依次读：AGENTS → 本规范/主要求 → 产品要求 → DEV_PROGRESS → LOG → GITHUB_ROLLBACK → TEST_METRICS → WORKFLOW → 当前层进度 → HANDOFF；需要时读架构/层合同/工具规则及方案 V3。规范修改后更新摘要，后续 Agent 不依赖聊天记录。

Phase 0 骨架仅 README、.gitignore、.env.example、依赖/检查配置和 server/firmware/frontend/tools 的目录职责说明；可建立检查工具，但不创建空业务模块冒充功能。frontend 仅目录说明，不生成 Vite 工程；Android 仅文档。

.env.example 仅空值占位，不含凭据。忽略 venv、缓存、.env 及真实配置、node_modules、dist/build/.pio、模型/weights/datasets/recordings、密钥和本地日志等；明确允许提交 .env.example。大文件用外部存储并记录来源/版本/哈希，不能删除用户原文件代替忽略。

## 十五、施工闭环与测试

每轮：读规范 → 检查工作区 → 写计划及层计划 → 备份 push 成功 → 施工 → 记录修改 → 测试 → 失败修复复测 → 文档漂移检查 → 更新日志/进度/层记录/HANDOFF/MEMORY → 审查提交 → push 核验 → 审计收尾 → 报告。

Phase 0 最低验证：在项目 venv 安装最小锁定依赖、执行真实 lint、git diff --check；如已有测试则运行测试。尚无单测时明确写“尚未建立单元测试”，不能用占位脚本输出成功代替通过。只做文档修订的轮次执行文档检查并如实说明未做依赖/lint/业务测试。

测试失败必须保留首次命令/错误、原因、修复、复测命令和最终状态；失败时停止扩展。未复测不写通过；无法修复记录阻塞。可按授权用 wip/failing-phase<N>-<time> 保存失败现场，不将其合入 main 冒充可用。

后续固件编译、协议/业务单测、前端构建、回放与实机测试按阶段增加；缺少硬件/模型/许可时只能标未验证或阻塞，不能用模拟数据充当实测。

## 十六、文档漂移检查

检查真实目录/依赖方向/版本/Phase 是否与文档一致，USB/无盲道斑马线/无 SLAM/手部融合/前端合同是否落实，模型隔离和许可是否满足，身份/Remote 是否正确，是否有秘密、大文件、未知用户改动和越阶段实现。

发现漂移修代码或规范并记原因，重新执行相关验证。DEV_PROGRESS、LOG、HANDOFF、层进度和 MEMORY 保留追加历史；当前状态摘要可更新，但旧事实不得改写成未发生。

## 十七、提交、推送与审计收尾

提交前检查 status --short、diff --check、diff --stat、diff，以及暂存区 diff；显式列文件暂存，不未经审查 git add .。仅提交本轮经审查内容，不包含密钥、模型、数据、环境或构建产物。

提交信息反映实际变更；main push 失败保留本地提交并记录，禁止强推，不宣布完整交付。

为消除原规范“最终哈希写入自身提交”的循环，采用两段收尾：

1. **施工交付提交 A**：代码/规范/验证记录及交接，push 后核验远端哈希。
2. **审计记录提交 B（必要时）**：LOG/HANDOFF 记录 A 的完整哈希及已核验 push 结果，提交并 push；B 的自身哈希不写进 B 内容。B 的远端状态在终端核验并向用户报告，下一轮日志补记 B。

最终报告分别列 A/B、远端核验、git status；不反复为记录自身哈希产生提交。B push 失败也如实标明。审计文件仍有未提交修改时不得声称工作区干净。

## 十八、回滚、交接和最终汇报

回滚优先 git revert 明确的错误提交；多个提交先检查顺序/合并关系再逐项操作，不默认执行危险范围回滚或 reset --hard。记录是否需要回滚、目标、建议命令及回滚后依赖/lint/相关测试。

LOG 每轮包含计划、实际修改/文件、所有验证、失败与复测、指标判断、漂移、Git 状态、回滚、风险、下一步。

HANDOFF 包含当前状态、本轮完成、未完成、下次任务、必读文档/关键文件、测试基线、仓库/remote/分支/基线/备份/A/B/push/工作区状态及风险。即使阻塞也记录真实停止点，不写“下一轮 Phase 1”掩盖 Phase 0 未完成。

停止本轮启动的长跑进程，但不终止用户原有进程。最后回复以“报告长官！！”开头，按实际情况列：本轮状态、环境与 GitHub、已完成、修改文件、测试、文档漂移、未完成、下一步、风险。纯文档修订可合并压缩栏目，但不能暗示完成 Git/代码施工。

## 十九、本次 V3 修订记录

| 原条款问题 | 本版最终处理 |
| --- | --- |
| 引用方案 V2 为技术总纲 | 改为方案 V3；V2 仅历史 |
| 保留盲道/斑马线对照 | 明确删除运行模块/状态/接口/页面/模型加载 |
| 旧推流及框架默认值 | 原生 USB CDC、ESP-IDF/esp_tinyusb；不默认 Arduino 框架 |
| 手部逻辑未包含 ToF 和新模型异步结果 | 加入标定、时间/帧号、归属、遮挡降级和确认合同 |
| 前端缺少正式施工阶段 | Phase 2B，电脑重设计、手机 Phase 6 无障碍界面 |
| 固件禁止一切决策但要求基础告警 | 明确仅有电时的有限 ToF 基础告警例外，禁止模型/导航决策 |
| 备份分支对未提交文档覆盖不清 | 明确备份只覆盖提交，列出未覆盖资料及准备文档基线 |
| push 后写最终哈希导致循环 | A 交付+B 审计，报告核验 B，不要求 B 自含哈希 |
| 文档尾部自动开工与用户本轮任务混淆 | 根据用户当前指令和实际 Phase 继续，不以文件文字自动执行 |

详细本轮修改及已做/未做检查见 docs/construction/LOG.md。后续主要求文件必须从本版整理，不再从旧提示词复制恢复旧条款。
